# commontrace-v2 — Python dependencies
# Minimum Python: 3.10
#
# Attention layer (memory/attention/) — required for semantic pre-filtering (Phase 0 / Alpha):
numpy>=1.24.0,<3.0
# See pyproject.toml's [attention] extra for why this floor is >=6.0/>=5.5.0
# (pip-audit found 5 known transformers RCE-class CVEs below transformers 5.5.0).
sentence-transformers>=6.0.0,<7.0   # pulls torch CPU automatically
transformers>=5.5.0,<7.0   # capped alongside sentence-transformers -- see pyproject.toml's [attention] extra
PyYAML>=6.0,<7.0

# Benchmark (commontrace/reference/measure_performance.py):
# PyYAML listed above; all other deps are stdlib (argparse, glob, json, re, datetime, os, sys)
